const mongoose= require("mongoose")
    const movieSchema = new mongoose.Schema({
        name : String,
        age: Number,
        email: String,
        movie_id: String,
    })

    
module.exports = mongoose.model("Movie", movieSchemaSchema)